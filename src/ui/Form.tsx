import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type FormProps = {
  children: any;
  onChange: (value: string) => void;
  value: string;
};

// const Form = ({ children, onChange, value }: FormProps) => {
//   return (

//   );
// };

// export default Form;

const styles = StyleSheet.create({
  input: {},
});
